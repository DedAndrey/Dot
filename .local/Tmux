### KEYS tmux###

# Перезагрузка .tmux.conf
prefix r

## List of plugins
# установить новые плагины
prefix + I
# обновить плагины
prefix + U
# удалить плагины, которых нет в списке
prefix + alt + u

## Tmux открытие текста
# открыть выделенный текст в программе по умолчанию
o
# открыть выделенный текст в $EDITOR
Ctrl-o
# искать выделенный текст в поисковике
Shift-s

## tmux-yank
# копировать текст из командной строки в буфер обмена. Normal Mode
prefix–y
# копировать выделенное в буфер обмена. Copy Mode
y
# "вставить" выделенное. Эквивалент копирования выделенного и вставки в командную строку. Copy Mode
Y (shift-y)

## Tmux copycat
# простой поиск файла
prefix + ctrl-f
# переход между git status files (best used after git status command)
prefix + ctrl-g
# переход между SHA-1/SHA-256 hashes (best used after git log command)
prefix + alt-h
# поиск url поиск (http, ftp и git urls)
prefix + ctrl-u
# номерной поиск (mnemonic d, as digit)
prefix + ctrl-d
# поиск ip адресов
prefix + alt-i
# перейти на следующее вхождение
n
# перейти на предыдущее вхождение
N
# копировать подсвеченное вхождение, если используется vi mode
Enter

## Tmux "воскрешение"
# сохранить
prefix + Ctrl-s
# восстановить
prefix + Ctrl-r

## Tmux сессии
# показать имена сессий и переключиться на одну из них
prefix + g
# диалог создания новой сессии
prefix + C
# убить текущую сессию без отключения от нее
prefix + X
# переключиться на последнюю сессию
prefix + S
# выкинуть текущую панель в новую сессию
prefix + @
# маркировать текущую панель
prefix + m
# присоединить маркированную панель к текущей сессии/окну и переключиться на нее
prefix + t <secondary-key>
# secondary-keys:
# присоединить горизонтально
secondary-keys: h, -, ":
# присоединить вертикально
secondary-keys: v, |, %:
# присоединить полным экраном
secondary-keys: f, @:

## Tmux управление панелями
## Навигация
# перейти на панель слева
prefix + h and prefix + C-h
# перейти на панель над текущей
prefix + j and prefix + C-j
# перейти на панель под текущей
prefix + k and prefix + C-k
# перейти на панель справа
prefix + l and prefix + C-l
## Изменение размеров панелей
# изменить размер панели на 5 пунктов влево
prefix + shift + h
# изменить размер панели на 5 пунктов вниз
prefix + shift + j
# изменить размер панели на 5 пунктов вверх
prefix + shift + k
# изменить размер панели на 5 пунктов вправо
prefix + shift + l

## Разделение панелей
# разбить панель горизонтально на полную высоту
prefix + |
# разбить панель вертикально
prefix + -
# разбить панель горизонтально
prefix + С-|
# разбить панель вертикально на полную ширину
prefix + С-_

## Tmux логирование
# переключить состояние логирования текущей панели
prefix + shift + p
# сохранить историю панели в файл
prefix + alt + shift + p
# очистить историю панели
prefix + alt + c
